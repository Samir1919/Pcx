# API

The API validates identity, role, ownership, state, authoritative totals, and idempotency. Clients receive purpose-built DTOs and never unrestricted persistence objects. Sensitive fields are omitted, not merely hidden by UI. External callbacks are authenticated, replay-safe, and reconcilable.

Full source: `../specifications/API_SPECIFICATION_STATE_MACHINES.md`.
