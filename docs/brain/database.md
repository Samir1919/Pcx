# Database

PostgreSQL is the relational source of truth. Use database constraints for identity, uniqueness, and integrity. Migrations are forward-reviewed and expand-contract by default. Destructive migrations are hard stops. Preserve serial-level inventory history, immutable order snapshots, and append-only audit facts.

Full source: `../specifications/DATABASE_ERD.md`.
