catalog: full-PC build create flow + storefront component summary

- Domain: createProductModelComponent (build -> part link with quantity).
- Backend: atomic build-create (POST /api/v1/admin/product-model-builds) that
  creates the BUILD model, any inline component parts with typed specs, and the
  product_model_components links in one transaction; server resolves each
  role's component category from the sell-flow build template and validates
  existing parts against it. listBuildRoles + listModelComponents added.
- Admin: new "PC builds" tab with a slot-based configurator (each component
  slot selects an existing part or creates a new one inline).
- Storefront: the model detail page renders a build's server-derived component
  summary (Included components).
- Fix the pre-existing admin build blocker by moving AuthProvider out of the
  root layout (login/register now live under an (auth) route group), so the
  /_global-error prerender no longer hits a useContext-null crash.

ADR 0017; handoff docs/handoffs/CATALOG_SEPARATE_PART_AND_BUILD.md.
