{
  "scope": "Admin payment/notification providers: delete credentials (hard delete)",
  "headed": true,
  "tool": "Playwright MCP browser (headed)",
  "result": "passed",
  "businessFlow": {
    "subject": "Provider config panels now offer Delete credentials (DELETE /admin/:providers/:provider/config/:mode) that hard-removes a stored provider+mode config",
    "steps": [
      "sign in as demo admin",
      "open /payments",
      "save sandbox bKash credentials via the form",
      "verify the Delete credentials button appears",
      "click Delete credentials and confirm the config is removed (button gone, 'No sandbox credentials configured yet')"
    ]
  },
  "notes": "Removing an active config leaves the provider with no active credentials (consumers fail closed). Same DELETE route + UI pattern added to notification providers. Fixed a pre-existing async currentTarget.reset() bug in both provider save forms.",
  "ranAt": "2026-09-01T07:10:00.000Z"
}
